# OS
Thumbs.db
.DS_Store

# Logs / caches
.dist
.tmp
.lock
.sass-cache
npm-debug.log
yarn.lock

# Dependencies
node_modules
package-lock.json

# Hugo build artifacts
builds
public
resources
.hugo_build.lock
hugo_stats.json

# Go module cache (Hugo modules)
go.sum

# Editor / tool
jsconfig.json
.cursorindexingignore

# Impeccable skill — linked locally via sync-upstreams (../impeccable)
.cursor/skills/impeccable
