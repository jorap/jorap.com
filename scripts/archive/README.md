One-shot migration scripts kept for history. Not wired into `package.json` or deploy.

Run from repo root, e.g. `python3 scripts/archive/apply-jesus-quotes.py`.
`seed-note-shareable-thought.py` adds `scripts/` to `sys.path` so it can import `notes_content`.
