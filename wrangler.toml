# Cloudflare Pages — output dir for the V2 build system.
# Dashboard build command: pnpm install --frozen-lockfile && pnpm run deploy
# Dashboard env: HUGO_VERSION=0.163.2, GO_VERSION=1.24.2, NODE_VERSION=22
name = "jorap-com"
pages_build_output_dir = "./public"
compatibility_date = "2026-06-15"
